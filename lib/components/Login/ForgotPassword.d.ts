import React from "react";
import { StaticImageData } from "next/image";
interface Props {
    logo: StaticImageData;
}
export default function ForgotPassword({ logo }: Props): React.JSX.Element;
export {};
