import React from "react";
interface TooltipProps {
    message: string;
    children: React.ReactNode;
}
export declare function Tooltip({ message, children }: TooltipProps): React.JSX.Element;
export {};
