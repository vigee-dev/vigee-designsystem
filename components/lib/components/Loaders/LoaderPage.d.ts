import { StaticImageData } from "next/image";
interface Props {
    text?: String;
    logo?: StaticImageData;
    borderColor?: String;
}
export declare const LoaderPage: ({ text, logo, borderColor }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
