import { StaticImageData } from "next/image";
interface Props {
    text?: String;
    logo?: StaticImageData;
    borderColor?: String;
}
export declare const Loader: ({ text, logo, borderColor }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
